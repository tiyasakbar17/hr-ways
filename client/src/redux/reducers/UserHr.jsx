const innitialState = {
	data: [],
	totalPage: null,
	pageNow: null,
};

const UserHr = (state = innitialState, action) => {
	const { type } = action;
	switch (type) {
		default:
			return state;
	}
};

export default UserHr;
