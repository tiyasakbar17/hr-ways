import React from "react";
import { connect } from "react-redux";

const Template2 = ({}) => {
	return <div>temlpate 2</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(Template2);
